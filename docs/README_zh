## Auto Mac Layout

Auto Mac Layout 是一个轻量级 macOS 菜单栏工具，用于根据当前显示器拓扑，自动保存并恢复 Finder 桌面图标位置。

当你插拔显示器、切换镜像、改变排列后，Finder 常常会打乱桌面图标。本工具会为每一种显示器组合生成指纹，并应用对应的图标布局。

## 功能特性

- 常驻菜单栏，不显示 Dock 图标。
- 按显示器布局分别保存桌面图标坐标。
- 显示器变更后自动恢复（带防抖延迟）。
- 菜单手动触发“保存当前布局”。
- 桌面文件集合变化时自动保存。
- 支持“登录时启动”。
- 配置文件采用原子写入，降低损坏风险。

## 运行要求

- macOS（依赖 AppleScript 控制 Finder）
- Rust 工具链（仅源码构建需要）

## 安装与运行

### 方式一：源码构建

```bash
cargo build --release
```

可执行文件位置：

```text
target/release/auto-mac-layout
```

运行：

```bash
./target/release/auto-mac-layout
```

### 方式二：登录自启动

首次运行后，在菜单中勾选 `Launch at Login`。

## 权限说明（重要）

应用通过 AppleScript 调整 Finder 桌面图标位置，首次运行可能需要系统授权。

请允许：

- Automation（自动化）中对 Finder 的控制权限
- 在你的系统配置下，如有需要请同时允许 Accessibility（辅助功能）

若恢复失败，请检查：

- `系统设置 -> 隐私与安全性 -> 自动化`
- `系统设置 -> 隐私与安全性 -> 辅助功能`

## 菜单项说明

- `Save Current Layout`：保存当前显示器组合下的图标坐标。
- `Open Config File`：打开布局配置文件。
- `Switch Apply Delay`：切换恢复防抖延迟（`0ms`、`300ms`、`1000ms`、`2000ms`、`3000ms`）。
- `Launch at Login`：开启/关闭登录自启动。
- `Quit`：退出程序。

## 配置文件位置

布局配置文件默认位于：

```text
~/Library/Application Support/auto-mac-layout/layouts.json
```

内容是“显示器指纹 -> 图标坐标列表”的映射。

## 开发与检查

格式检查：

```bash
cargo fmt --check
```

编译检查：

```bash
cargo check
```

发布构建：

```bash
cargo build --release
```

## 常见问题

- 显示器变化后没有恢复：
	- 确认 Finder 桌面图标显示已开启。
	- 在该显示器组合下先执行一次 `Save Current Layout`。
	- 检查自动化/辅助功能权限。

- 配置文件为空：
	- 保存时桌面至少需要有一个项目。

- 程序运行但菜单栏图标异常：
	- 检查启动日志中是否出现图标加载失败信息。

## 隐私与安全

- 仅在本地保存桌面图标名称和坐标。
- 不包含网络上传或远程通信逻辑。

## 开源前检查清单

- [x] 功能、安装、使用文档完善。
- [x] 调试期日志输出已从源码中移除。
- [ ] 补充许可证文件（建议开源前完成）。
- [ ] 配置 CI（建议为 PR 增加自动检查）。

## 参与贡献

欢迎提交 Issue 和 Pull Request。

建议流程：

1. Fork 并创建功能分支。
2. 运行 `cargo fmt --check` 与 `cargo check`。
3. 在 PR 中说明变更行为和复现步骤。
